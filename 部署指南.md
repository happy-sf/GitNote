# GitNote 部署指南

## 项目架构分析

### 项目结构
```
GitNote/                    # Git项目目录（源代码）
├── main.py                 # 主程序入口
├── Init.py                 # 初始化逻辑
├── GitNote.py              # 主界面逻辑
├── GitNote.ui              # 界面设计文件
├── GitNoteUi.py            # 编译后的界面文件
├── askpass.py              # Git认证脚本
├── setup_git.py            # Git配置工具
├── run_gitnote.py          # 启动程序（推荐使用）
└── 其他工具脚本和图标文件

~/.GitNote/                # 用户数据目录（由程序自动创建）
├── config.json             # 界面配置（主题、字体等）
└── Notes/                  # 笔记存储目录（Git仓库）
    ├── .git/               # Git版本控制
    ├── README.md            # 初始文件
    └── 用户笔记文件.md
```

## 部署方案

### 方案一：完整部署（推荐）
**适用于：需要所有笔记数据的新设备**

#### 步骤1：部署源代码
```bash
# 1. 克隆项目到新设备
git clone https://github.com/happy-sf/GitNote.git
cd GitNote

# 2. 安装依赖
pip install PyQt5 mistune gitpython

# 3. 运行程序
python3 run_gitnote.py
```

#### 步骤2：迁移数据（重要）
```bash
# 1. 复制 .GitNote 目录到用户home目录
cp -r ~/.GitNote/Notes/ ~/   # 将现有笔记复制到新设备

# 或者如果是从零开始：
# 程序会自动在 ~/.GitNote/Notes/ 创建新仓库
```

### 方案二：只部署源代码
**适用于：新设备，从头开始使用**

#### 步骤1：部署源代码
```bash
# 1. 克隆项目到新设备
git clone https://github.com/happy-sf/GitNote.git
cd GitNote

# 2. 安装依赖
pip install PyQt5 mistune gitpython

# 3. 运行程序（程序会自动初始化新仓库）
python3 run_gitnote.py
```

### 方案三：选择性部署
**适用于：保留部分数据或需要特殊配置**

#### 步骤1：部署源代码
```bash
# 1. 克隆项目
git clone https://github.com/happy-sf/GitNote.git
cd GitNote
pip install PyQt5 mistune gitpython
```

#### 步骤2：配置Git仓库（可选）
```bash
# 如果需要同步到Gitee仓库
cd ~/.GitNote/Notes
python3 ~/GitNote/setup_git.py
```

## 关键说明

### 关于 ~/.GitNote 目录

#### 是否需要复制？
- **是**：如果要保留现有的所有笔记
- **否**：如果要从零开始使用

#### 目录作用：
- **config.json**: 存储界面配置（主题、字体等）
- **Notes/**: 笔记存储目录，是完整的Git仓库
  - 包含所有笔记文件
  - 包含Git版本历史
  - 包含远程仓库配置

### 依赖包清单
```bash
# 核心依赖
PyQt5              # GUI界面框架
mistune            # Markdown解析器
gitpython         # Git操作库

# 可选依赖
pdfkit            # PDF导出（如果需要导出功能）
wkhtmltopdf       # PDF渲染引擎（如果使用PDF导出）
```

### 注意事项

1. **Git仓库配置**：
   - ~/.GitNote/Notes/ 目录是Git仓库
   - 可能包含远程仓库配置（Gitee/GitHub）
   - 复制后会保留完整的版本历史

2. **配置文件**：
   - config.json 存储用户界面偏好
   - 复制后会保留主题、字体等设置

3. **权限问题**：
   - 确保对 ~/.GitNote 目录有读写权限
   - Git仓库需要正确设置SSH密钥（如果使用SSH）

## 快速部署脚本

```bash
#!/bin/bash
# GitNote快速部署脚本

# 1. 克隆项目
echo "正在克隆GitNote项目..."
git clone https://github.com/happy-sf/GitNote.git
cd GitNote

# 2. 安装依赖
echo "正在安装依赖..."
pip3 install PyQt5 mistune gitpython

# 3. 复制数据（可选）
echo "是否复制现有笔记？(y/N)"
read -r copy_data
if [[ "$copy_data" =~ ^[Yy]$ ]]; then
    cp -r ~/.GitNote/Notes/ ~/ 2>/dev/null || echo "未找到现有笔记，将创建新目录"
fi

# 4. 运行程序
echo "启动GitNote..."
python3 run_gitnote.py
```

## 常见问题

**Q: 部署后Git同步失败？**
A: 可能需要重新配置SSH密钥或远程仓库

**Q: 配置丢失？**  
A: 确保 ~/.GitNote/config.json 被正确复制

**Q: 笔记丢失？**
A: 确保 ~/.GitNote/Notes/ 目录被复制

**Q: 权限问题？**
A: 确保用户对 ~/.GitNote 目录有读写权限

## 总结

- **源代码**：只需要Git克隆和Python依赖
- **数据**：如果保留现有笔记，需要复制 ~/.GitNote/Notes/
- **配置**：自动创建，可以保留或重新设置
- **Git同步**：需要配置SSH密钥和远程仓库访问权限

这个项目的优势是数据与程序分离，部署简单且灵活。