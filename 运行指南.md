# GitNote 运行指南

## 错误已修复

我已经修复了之前遇到的错误：

1. **✓ markdownTxt 类型错误**
   - 添加了类型检查，确保处理的是字符串而不是列表
   - 使用 `str()` 强制转换避免类型错误

2. **✓ Git 远程仓库错误**
   - 添加了异常处理，当没有远程仓库时不会崩溃
   - 只有在有远程仓库时才尝试同步

## 运行方法

### 方法1：使用完整功能版（推荐）
```bash
python3 run_gitnote.py
```
这个版本会：
- 自动初始化本地Git仓库
- 提示您配置远程仓库（可选）
- 保留所有优化功能

### 方法2：使用原始版本
```bash
python3 main.py
```
如果已经配置过GitNote，可以使用这个版本。

## 功能说明

### 界面优化功能
- ✅ 按钮尺寸增大到32x32像素
- ✅ 按钮按功能使用不同颜色
- ✅ 左侧面板可以自由拖拽调整宽度
- ✅ 按钮间距增大到10像素
- ✅ 所有按钮都有悬停和点击效果

### 笔记功能
- ✅ 创建和编辑笔记
- ✅ 保存笔记到本地
- ✅ 文件夹管理
- ✅ Markdown实时预览
- ✅ 插入图片功能
- ✅ 导出PDF功能

### Git功能（可选）
- ✅ 本地Git仓库管理
- ⚠️ 远程同步需要配置远程仓库地址

## 常见问题

**Q: 出现 Git 相关错误怎么办？**
A: 可以忽略，程序仍可正常使用。只有需要云同步时才需要配置Git。

**Q: 如何配置远程仓库？**
A: 在Git仓库目录（~/.GitNote/notes）下运行：
```bash
git remote add origin <您的仓库地址>
```

**Q: 按钮功能不正常？**
A: 确保使用了 `run_gitnote.py` 启动程序。

## 界面效果说明

- 🔵 **蓝色按钮**：更新/同步功能
- 🟢 **绿色按钮**：保存笔记
- 🟠 **橙色按钮**：插入图片
- ⚫ **灰色按钮**：配置选项
- 🟣 **紫色按钮**：导出功能

现在程序应该可以正常运行了！